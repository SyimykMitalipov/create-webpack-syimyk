


const defaultState = {
    items: [],
    isFetching: false,

}
export default function responseReducer(state = defaultState , action) {
    switch(action.type){
        default:
            return state
    }
}